import styled from "styled-components";

const ChatStyles = styled.div`
    border: 2px solid green;
`;

const Chat = () => {
    return (
        <ChatStyles>
            <div>Chat</div>
        </ChatStyles>
    );
};

export default Chat;